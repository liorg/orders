@model StatusVm
<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title"><b>אישור הזמנה טרם העברתו למשלוח</b></h3>
            </div>
            <div class="panel-body">
                @*<div class="btn-group">
                    <a class="btn btn-lg btn-success" href="@Url.Action("ApprovalRequest", "Status", new { id = Model.ShipId })">
                        <span class="visible-md visible-lg"><i class="fa fa-paper-plane fa-2x pull-right"></i>שלח</span><i class="visible-xs visible-sm fa fa-paper-plane fa-2x pull-right"></i> </a>
                </div>*@
            <div class="btn-group">
                    <a class="btn btn-lg btn-danger"
                         href="@Url.Action("OrderItem", "Offer", new { shipId = Model.ShipId })">
                        <span class="visible-md visible-lg"><i class="fa  fa-paper-plane fa fa-2x pull-right"></i>בקשת הזמנה</span><i class="visible-xs  visible-sm fa  fa-paper-plane   fa-2x pull-right"></i>  </a>
                </div>
                <div class="btn-group">
                    <a class="btn btn-lg btn-default" href="@(Url.Action("Edit", "S", new { id = Model.ShipId }))">
                        <span class="visible-md visible-lg"><i class="fa fa-pencil fa-2x pull-right"></i>ערוך </span><i class="visible-xs visible-sm fa fa-pencil fa-2x pull-right"></i></a>
                </div>
              @*  <div class="btn-group">
                    <a class="btn btn-lg btn-danger" href="@(Url.Action("RemoveItem", "Status", new { id = Model.ShipId }))">
                        <span class="visible-md visible-lg"><i class="fa  fa-times fa-2x pull-right"></i>ביטול הזמנה </span><i class="visible-xs visible-sm fa fa-times fa-2x pull-right"></i></a>
                </div>*@
            </div>
        </div>
    </div>
</div>
